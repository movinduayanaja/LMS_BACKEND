spring.application.name=backendlib

# --- MySQL datasource ---
spring.datasource.url=jdbc:mysql://localhost:3306/library_db1
spring.datasource.username=root
spring.datasource.password=adSQ**30
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Flyway ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# --- JWT settings (Day 9) ---
# Use a strong secret (>= 32 characters if plain text) OR a Base64-encoded 256-bit key.
app.jwt.secret=change-this-to-a-strong-256-bit-secret-value-please

# Token lifetime in milliseconds (1 hour here)
app.jwt.expiration-ms=3600000


# --- Mail ---
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=c1e0c2a2fcc8b0
spring.mail.password=facb4c9f533a03
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Optional timeouts
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000


# --- Multipart limits (tune as you like) ---
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# --- Where to store images (local path) ---
app.upload.dir=uploads
app.upload.books-subdir=book-covers
